import * as stagesRepository from '../../src/repositories/stages.repository';

jest.mock("../../src/repositories/stages.repository")



describe("Order stages test Suites",()=>{

     afterEach(() => {
    jest.clearAllMocks();
  });

  it("Should fetch all the orders stages",()=>{
    const mockStages=[
        
    ]
  })

})